{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "node backend/server.js & sleep 5 && python3 agents/pipeline.py --loop --interval 2",
    "restartPolicyType": "ALWAYS"
  }
}
